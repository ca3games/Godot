[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://2D/Mamerto 01/Front/front.png" type="Texture" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_alpha_prepass, unshaded;

uniform vec4 jacket:hint_color;
uniform sampler2D texture_albedo : hint_albedo;

void fragment()
{
	vec2 base_uv = UV;
	vec4 albedo_text = texture(texture_albedo, base_uv);
	ALBEDO = albedo_text.rgb + jacket.rgb;
	ALPHA = albedo_text.a;
}"

[resource]
shader = SubResource( 1 )
shader_param/jacket = Color( 1, 1, 1, 1 )
shader_param/texture_albedo = ExtResource( 1 )

